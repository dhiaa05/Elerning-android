<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".EnsLogInActivity">

    <ImageView
        android:id="@+id/image"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/img_login"
        android:layout_marginTop="80dp"

        />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/image"
        android:layout_marginBottom="50dp"
        android:id="@+id/ensNom"
        android:hint="Entrez Votre Nom du Utilisateur"
        android:inputType="text"
        />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/ensNom"
        android:layout_marginBottom="50dp"
        android:id="@+id/ensPass"
        android:hint="Entrez Votre Mots de Pass"
        android:inputType="textPassword"
        />
    <Button
        android:id="@+id/logenss"
        android:layout_width="320dp"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:layout_below="@id/ensPass"
        android:text="Log In"
        android:background="@color/vertapp"
        android:textColor="@color/color_text_button"
        android:onClick="ensLogin"
        />

</RelativeLayout>